# Automatically enter the nix development shell when entering this directory
# Requires direnv (https://direnv.net/) to be installed and set up

use flake

# Optional: Set environment variables for development
export LUCEE_DEV=1
export NIX_BUILD_CORES=0  # Use all available cores for builds

# Inform the user about available commands
echo "🚀 Lucee NixOS Module development environment loaded!"
echo "💡 Run 'validate-all' to test the module"